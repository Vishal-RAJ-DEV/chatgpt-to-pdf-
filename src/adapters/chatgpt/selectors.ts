/**
 * ChatGPT DOM Selectors — Phase 1 placeholder.
 *
 * This module will contain the centralized selector registry and
 * multi-tier fallback strategies for locating ChatGPT conversation
 * elements in the DOM.
 *
 * Selectors will be added in a later phase once real extraction
 * is implemented. No speculative selectors are included here.
 */
